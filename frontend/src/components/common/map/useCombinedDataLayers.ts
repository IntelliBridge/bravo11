// note(myles) might not need this, depends on how much work we wanna do in map component
import { DataLayer } from "@/types/map";

export default function useCombinedDataLayers(layers: DataLayer[]) {

    
}